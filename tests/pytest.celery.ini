[pytest]
# 集成测试需要真实的 Celery worker 和 Redis 环境
# 默认运行单元测试，跳过集成测试
markers =
    integration: 需要真实环境的集成测试（需要 Redis + Celery worker）
    unit: 单元测试（不需要真实环境）

# 默认跳过集成测试
addopts =
    -v
    --tb=short
    -m "not integration"
